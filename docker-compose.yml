services:
  bigtech-copilot:
    build:
      context: .
      network: host
    image: bigtech-copilot:latest
    container_name: bigtech-copilot
    restart: unless-stopped
    ports:
      - "127.0.0.1:8091:3000"
    environment:
      - PORT=3000
      - OPENCODE_MODEL=opencode/mimo-v2.5-free
