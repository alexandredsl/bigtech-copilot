services:
  bigtech-copilot:
    build: .
    container_name: bigtech-copilot
    restart: unless-stopped
    ports:
      - "8091:3000"
    environment:
      - OPENCODE_API_KEY=${OPENCODE_API_KEY}
      - PORT=3000
